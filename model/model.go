package model

type ResultSet struct {
}

func Init() error {
	// set up database connection
}

func Lookup(qry []string) (ResultSet, error) {
	// assemble the query
	// execute query
	// format results back

	return Results{}, nil
}
